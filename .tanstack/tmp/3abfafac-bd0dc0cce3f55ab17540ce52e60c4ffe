import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/issue/$owner/$repo/$number')({
  component: RouteComponent,
})

function RouteComponent() {
  return <div>Hello "/issue/$owner/$repo/$number"!</div>
}
